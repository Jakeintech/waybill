{
  "description": "SessionEnd queues session metadata and spawns the detached, dependency-free miner, which meters the transcript into the ledger immediately (skipped entirely while metering is paused). SessionStart surfaces at most one line: a budget-pacing notice on a fresh threshold, or a one-shot first-run pointer (config.notices.level governs both). Neither ever blocks or slows a session.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/scripts/session-start.sh\"",
            "timeout": 10
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/scripts/capture-session.sh\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
