Pass if the assistant loads the waybill `report` skill and builds the pitch
from engine numbers (`query report`), one page, with a spend-ledger section
and evidence-tier footnote. Fail if it invents numbers, collapses ranges to
midpoints, or never engages the waybill plugin.
